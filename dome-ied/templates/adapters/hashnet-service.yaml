{{- if .Values.adapters.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dome-ied.hashnet.fullname" . }}
  labels:
    {{- include "dome-ied.labels" . | nindent 4 }}
    app.kubernetes.io/component: hashnet-adapter
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dome-ied.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: hashnet-adapter
{{- end }}
